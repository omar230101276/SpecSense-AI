version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: specsense-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: specsense_db
      POSTGRES_PASSWORD: specsense_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
